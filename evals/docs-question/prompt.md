---
name: "docs-question"
runs: 2
max_turns: 15
timeout_seconds: 300
allowed_tools: ["Bash", "WebSearch", "WebFetch", "Skill", "Read"]
---
What does the `--jq` flag on `gh api` do, and can it take a jq file instead of an inline expression?
